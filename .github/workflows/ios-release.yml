name: iOS TestFlight

# Builds a signed iOS IPA and uploads to TestFlight on every push to master.
# One-time Apple + GitHub secrets setup: docs/ios-releases.md
#
# Runs in parallel with release.yml (macOS) and Vercel (PWA). Same version
# stamp scheme: <major>.<minor>.<commit_count> from package.json + the total
# commit count on this ref (git rev-list --count HEAD) — identical to
# release.yml's, so a desktop and an iOS build cut from the same commit
# always carry the same version/build number, even though each workflow can
# be independently skipped by the platform-reachability check.
#
# The `changes` job below decides whether this push can only have affected
# desktop — if so, the (expensive, macOS-runner) TestFlight build is skipped.
# It's driven entirely by scripts/platform-reachability.mjs, which traces the
# real static import graph from the two shell entry points rather than a
# hand-maintained path list, so it self-updates as the app grows — see that
# script's header for the full explanation. workflow_dispatch always builds
# regardless (a manual run should never be silently skipped).
on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

env:
  CI: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.decide.outputs.should_run }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - id: decide
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" != "push" ] || [ "${{ github.ref_type }}" != "branch" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "Manual dispatch — always building."
            exit 0
          fi
          BEFORE="${{ github.event.before }}"
          ZEROS="0000000000000000000000000000000000000000"
          if [ -z "$BEFORE" ] || [ "$BEFORE" = "$ZEROS" ] || ! git cat-file -e "${BEFORE}^{commit}" 2>/dev/null; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "No usable base commit to diff — building to be safe."
            exit 0
          fi
          if git diff --name-only "$BEFORE" "${{ github.sha }}" \
              | node scripts/platform-reachability.mjs check mobile; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

  testflight:
    needs: changes
    if: needs.changes.outputs.should_run == 'true'
    concurrency:
      group: ios-testflight
      cancel-in-progress: true
    runs-on: macos-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Compute version
        # BUILD_NUMBER comes from the commit count on this ref (git rev-list
        # --count HEAD), not this workflow's own github.run_number. This
        # workflow and release.yml (desktop) each have an independent run
        # counter and each can be skipped by the platform-reachability check,
        # so run_number drifts between them over pushes. Commit count is the
        # same for both workflows on the same commit, so desktop and iOS
        # builds cut from the same commit always carry the same number.
        run: |
          BUILD_NUMBER=$(git rev-list --count HEAD)
          if [ "${{ github.ref_type }}" = "tag" ]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            MAJOR_MINOR=$(node -p "require('./package.json').version.split('.').slice(0,2).join('.')")
            VERSION="${MAJOR_MINOR}.${BUILD_NUMBER}"
          fi
          node -e "for (const p of ['src-tauri/tauri.conf.json','package.json']) { const fs=require('fs'); const c=JSON.parse(fs.readFileSync(p)); c.version='$VERSION'; fs.writeFileSync(p, JSON.stringify(c,null,2)+'\n'); }"
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "BUILD_NUMBER=$BUILD_NUMBER" >> "$GITHUB_ENV"
          echo "Building iOS version $VERSION (build $BUILD_NUMBER)"

      - name: Configure App Store Connect API key
        env:
          APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
          APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
          APPSTORE_PRIVATE_KEY: ${{ secrets.APPSTORE_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${APPSTORE_KEY_ID:-}" ] || [ -z "${APPSTORE_ISSUER_ID:-}" ] || [ -z "${APPSTORE_PRIVATE_KEY:-}" ]; then
            echo "::error::Missing App Store Connect API secrets. See docs/ios-releases.md § One-time setup."
            exit 1
          fi
          KEY_DIR="${HOME}/.appstoreconnect/private_keys"
          mkdir -p "$KEY_DIR"
          KEY_PATH="${KEY_DIR}/AuthKey_${APPSTORE_KEY_ID}.p8"
          echo "$APPSTORE_PRIVATE_KEY" | base64 --decode > "$KEY_PATH"
          chmod 600 "$KEY_PATH"
          echo "APPLE_API_KEY=${APPSTORE_KEY_ID}" >> "$GITHUB_ENV"
          echo "APPLE_API_ISSUER=${APPSTORE_ISSUER_ID}" >> "$GITHUB_ENV"
          echo "APPLE_API_KEY_PATH=${KEY_PATH}" >> "$GITHUB_ENV"

      - name: Initialize iOS project
        env:
          APPLE_DEVELOPMENT_TEAM: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          if [ -z "${APPLE_DEVELOPMENT_TEAM:-}" ]; then
            echo "::error::Missing APPLE_TEAM_ID secret. See docs/ios-releases.md."
            exit 1
          fi
          npm run tauri ios init -- --ci
          bash scripts/ios-postinit.sh

      - name: Build signed IPA
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          APPLE_DEVELOPMENT_TEAM: ${{ secrets.APPLE_TEAM_ID }}
          IOS_CERTIFICATE: ${{ secrets.IOS_CERTIFICATE }}
          IOS_CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          IOS_MOBILE_PROVISION: ${{ secrets.IOS_MOBILE_PROVISION }}
        run: |
          set -euo pipefail
          # Empty IOS_* secrets become "" in GHA and Tauri still tries security
          # import — which fails. Unset unless all three manual-signing values exist.
          if [ -z "${IOS_CERTIFICATE:-}" ] || [ -z "${IOS_CERTIFICATE_PASSWORD:-}" ] || [ -z "${IOS_MOBILE_PROVISION:-}" ]; then
            unset IOS_CERTIFICATE IOS_CERTIFICATE_PASSWORD IOS_MOBILE_PROVISION
            echo "Using automatic signing (App Store Connect API key)."
          else
            echo "Using manual signing (IOS_CERTIFICATE + provisioning profile)."
          fi
          npm run tauri ios build -- --export-method app-store-connect --ci --build-number "$BUILD_NUMBER"

      - name: Upload to TestFlight
        env:
          APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
          APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
        run: |
          set -euo pipefail
          IPA=$(find src-tauri/gen/apple/build -name '*.ipa' -print | head -1)
          if [ -z "$IPA" ]; then
            echo "::error::No IPA found under src-tauri/gen/apple/build"
            find src-tauri/gen/apple -name '*.ipa' -o -name '*.xcarchive' 2>/dev/null || true
            exit 1
          fi
          echo "Uploading $IPA (v${APP_VERSION}, build ${BUILD_NUMBER})"
          xcrun altool --upload-app \
            --type ios \
            --file "$IPA" \
            --apiKey "$APPSTORE_KEY_ID" \
            --apiIssuer "$APPSTORE_ISSUER_ID"
