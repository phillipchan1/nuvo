{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Nuvo",
  "windows": ["main", "spotlight"],
  "permissions": [
    "core:default",
    "updater:default",
    "shell:default",
    "process:default"
  ]
}
